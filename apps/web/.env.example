# Dev proxy target for /api (Vite). Prod uses same-origin behind Caddy.
VITE_API_PROXY=http://localhost:8080

# Optional API key sent to the gateway (matches GATEWAY_API_KEY) for REST + WS
VITE_API_KEY=
